# Copy to .env.local and fill in. .env.local is gitignored.
# Password for the /demo admin preview gate. Leave empty to disable the gate.
# Note: Vite inlines VITE_* vars into the client bundle, so this is visible to
# anyone who downloads the built JS. Use only as a casual deterrent.
VITE_ADMIN_PASSWORD=
